# Workflows README

Workflows are ai-driven and process-oriented. They contain detail on the sequence to achieve a goal.
An AI tool can review a workflow to see the 'algorithm' or 'pattern'

Workflow = “What steps do we follow?”